1.主键（under INNODB）--候选键中的一个
主键不能为空，一个数据列只有一个主键。
如果我们定义了主键，INNODB会选择用它来建立聚集索引。
如果为定义，INNODB选择不含NULL值的第一个唯一索引作为主键索引。
如果不存在这样的索引，INNODB选择内置6字节长的ROWID作为隐含的聚集索引。（不可引用）
B+树的叶子结点内数据记录按主键顺序存放，INNODB自动插入适当位置。达到装载因子（默认15/16）则开辟新结点。
  装载因子：用的/总数
  
若使用非自增主键，例如身份证号，每次插入的值近似于随机，故每次插入的位置可能在现有索引叶中间的某个位置，因此MYSQL不得不为了插入而移动数据，甚至目标页面
可能已经被从缓存清除，不得不从磁盘读回，增加碎片，后期不得不用optimize table重建表并且优化填充页面。
  optimize table table_name; MYISAM
  alter table tabel_name engine = 'innodb'; INNODB官方推荐优化方式

2.超键
关系中能唯一标识元组的属性集称为关系模式的超键。
3.候选键
不含有多余属性的超键

例子
学生（学号，姓名，性别，身份证号，教师编号）表

超键：包含学号或者身份证号的任意组合。（学号），（学号，姓名），（身份证号，性别）

候选键：（学号），（身份证号）。
