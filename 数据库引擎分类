1.ISAM引擎
  特点：多叉平衡树，树矮（减少IO），节点记录多（一次性读取更多）。读取操作快，不占用大量内存和存储资源。
  缺点：不支持事务，不支持外键，不支持索引，不能够容错。
2.MYISAM引擎
  Mysql 的ISAM扩展格式，提供索引，字段管理，表级锁功能。强调快速读取功能。没有外键，没有事务。
3.Heap引擎
  将数据存储在内存中，哈希索引，无需iO，插入，查询，更新效率极高。不稳定，数据容易丢失。
4.Innodb引擎
  B+树存储，比ISAM MYISAM慢，但是提供ACID特性，事务支持，系统崩溃修复能力，行级锁，外键约束。数据与索引存放在一起。
  
5.MYISAM与Innodb对比

INNODB 在内存中建立缓冲池来缓存 数据和索引，所以需要更多的内存和存储。
MYISAM 可被压缩，存储空间小，支持三种存储格式，静态表，动态表，压缩表。压缩表是只读的。

INNODB 索引使用B+树，树的非叶子结点存储的是主键，叶子结点存储主键和数据。
MYISAM 也使用B+树，但是叶子结点存储的是数据的地址。

INNODB 索引等表信息和数据存放到一个表空间中，一个表可以跨很多个文件。数据量几十G时备份很难。
MYISAM 一个表分隔存储，.frm存储表定义 .MYD存储表数据 .MYI存储表索引。因为数据单独存储，跨平台转移方便。

INNODB行级锁，模糊查询时表级锁
MYISAM表级锁

INNODB不保存表的行数
MYISAM保存表的行数
